import React from 'react';

const Description = () => {
  return (
    <div style={{ padding: '10px', border: '1px solid #000', width: '200px' }}>
      <h1>Description</h1>
      <p>
        This is a simple example of an express application serving a
        server-side-rendered React app using Node Express, Webpack and Babel to
        transpile some simple React components into static rendered HTML.
      </p>
    </div>
  );
};

export default Description;
